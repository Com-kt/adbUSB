name: Update

on:
  push:
    branches: [ "update" ]
  pull_request:
    branches: [ "update" ]

workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Update
        run: echo Update, file!
